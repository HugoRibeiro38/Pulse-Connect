import { Fragment } from 'react';

type TestingProps = {
	//
};

const Testing: React.FunctionComponent<
	TestingProps
> = ({}): React.ReactNode => {
	return (
		<Fragment>
			<h1>Testing</h1>
			<p>Testing page</p>
		</Fragment>
	);
};

export default Testing;
